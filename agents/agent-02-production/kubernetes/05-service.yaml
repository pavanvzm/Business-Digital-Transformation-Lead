apiVersion: v1
kind: Service
metadata:
  name: agent-02-service
  namespace: manufacturing-mas
  labels:
    app: manufacturing-mas
    agent: agent-02
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http-health
  selector:
    app: manufacturing-mas
    agent: agent-02
